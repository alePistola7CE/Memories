// Import a library
import {
  AppRegistry
} from 'react-native';
import FullNavigator from './src/components/FullNavigator';

// Render it to the device
AppRegistry.registerComponent('Memories', () => FullNavigator);
